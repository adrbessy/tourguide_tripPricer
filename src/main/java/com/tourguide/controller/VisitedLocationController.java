package com.tourguide.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class VisitedLocationController {

  // private static final Logger logger =
  // LogManager.getLogger(VisitedLocationController.class);
  /*
   * @Autowired private VisitedLocationService visitedLocationService;
   */
  /**
   * Add a new visited location
   * 
   * @param visitedLocation An object bid
   * @return The visitedLocation object saved
   */
  /*
   * @PostMapping("/visitedLocation") public VisitedLocation
   * createVisitedLocation(@RequestBody Location location, @RequestParam String
   * userName) { VisitedLocation newVisitedLocation = null; try {
   * logger.info("Post request with the endpoint 'bid'"); newBid =
   * bidService.saveBid(bid); logger.info(
   * "response following the Post on the endpoint 'bid' with the given bid : {" +
   * bid.toString() + "}"); } catch (Exception exception) {
   * logger.error("Error in the BidRestController in the method createBid :" +
   * exception.getMessage()); } return newBid; }
   */

}
